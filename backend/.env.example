DATABASE_URL=postgresql://user:password@localhost:5432/expireease
PORT=4000
NODE_ENV=development
# Firebase Admin credentials for verifying Anurag's Auth tokens.
# Set via `gcloud auth application-default login` locally, or a
# service-account JSON path in production:
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
